# ImpulseLock V2 — Detailed Tasks

Granular, checkbox-level breakdown of [roadmap.md](./roadmap.md). Organized by phase, in dependency order. No code is written against this list yet — it's the implementation checklist for when building starts. Each item references the design doc that justifies it, so "why" is always one click away.

---

## Phase 0 — Foundation: JPA migration + normalized schema ✅ implemented

- [x] Add `flyway-core` + `flyway-mysql` dependencies to `pom.xml` (also swapped `spring-boot-starter-jdbc` for `spring-boot-starter-data-jpa`, and added `spring-security-crypto` for password hashing — see note below).
- [x] Set `spring.jpa.hibernate.ddl-auto=validate` in every profile (never `update`/`create`) — see [database-design.md](./database-design.md). (Only one profile exists as of Phase 0; per-profile split arrives in Phase 6.) Also set `spring.jpa.open-in-view=false`.
- [x] Author `src/main/resources/db/migration/V1__init_schema.sql`: `users`, `roles`, `user_roles`, `restricted_categories`, `transactions`, `rule_configs`, `refresh_tokens`, `audit_log` — full DDL per [database-design.md](./database-design.md). **Deviation**: `transactions.decision_type` / `risk_score` / `explanation` / `triggered_rules` are nullable in this migration, not `NOT NULL` — the JPA entity intentionally doesn't map them yet (see next item), so the DB constraint is loosened until Phase 2 wires the write path and a follow-up migration tightens it.
- [x] Author `V2__seed_roles_and_rule_configs.sql`: seed `ROLE_USER`/`ROLE_ADMIN`, seed `rule_configs` rows with V1-equivalent weights (70/40/30/25/20). **Note**: BLOCK/DELAY decision thresholds (80/40) are *not* seeded anywhere yet — `DecisionEngine` keeps them as hardcoded literals in Phase 0 (unchanged from V1); sourcing them from config is explicit Phase 2 work per this file's own Phase 2 section.
- [x] Create JPA entities: `User`, `Role`, `RestrictedCategory`, `Transaction`, `RuleConfig` (`RefreshToken`, `AuditLog` tables exist in the migration but have no entity yet, per plan). `Transaction` deliberately omits `decisionType`/`riskScore`/`explanation`/`triggeredRules` fields — Phase 0 matches V1's actual behavior (Decision computed and returned, not persisted on the row); Phase 2 adds those fields.
- [x] Add `@EntityListeners(AuditingEntityListener.class)` + `@CreatedDate`/`@LastModifiedDate` fields; enable via `@EnableJpaAuditing` in a `JpaAuditingConfig`.
- [x] Replace `UserRepository`/`TransactionRepository` (hand-written `JdbcTemplate`) with Spring Data JPA interfaces — see [v1/database.md](../v1/database.md) for the code replaced. Also added `RoleRepository`, `RestrictedCategoryRepository`, `RuleConfigRepository` (all needed once entities exist, trivial to add now).
- [x] Add `TransactionSpecifications` (`byUser`, `occurredBetween`, `byCategory`, `byMerchant`, `amountBetween`). **Deviation**: no `byDecisionType` yet — that field isn't mapped on the entity until Phase 2 (see above); a comment in the class marks where it goes.
- [x] Re-point `DecisionEngine`/rules/`TransactionService` at the new entities. `SpendingRule`/`AbstractSpendingRule`/all five rule classes now take `entity.Transaction`/`entity.User` (amount comparisons moved from primitive `double` to `BigDecimal.compareTo`). Old unauthenticated endpoints were kept working, but reshaped: `TransactionEvaluateRequest`/`UserUpsertRequest`/`UserProfileResponse` DTOs (in `dto/`) replace binding directly to `model.Transaction`/`model.UserProfile` (which are deleted — identity moved from a client-chosen `userId` string to a server-generated `id` + unique `username`, so byte-for-byte contract preservation wasn't possible or desirable). A new transitional `UserService` hashes passwords (`spring-security-crypto`'s `BCryptPasswordEncoder`) and seeds a default `"LUXURY"` restricted-category row per new user, replacing `CategoryRestrictionRule`'s old hardcoded fallback now that the association is a real child table. Also removed `ImpulseLockApplication`'s leftover startup demo block (see [v1/design-decisions.md](../v1/design-decisions.md) item 9) since it directly `new`-ed up the now-deleted model classes.
- [x] Repository-level tests: Testcontainers MySQL (`AbstractIntegrationTest` base class using `@ServiceConnection`), covering unique constraints (`username`, `email`), cascade delete (`restricted_categories`), auditing timestamps, and `TransactionSpecifications` filtering. **Not run in this environment** (no Docker available here) — verified to compile; needs a Docker-capable machine or CI to actually execute (see [deployment-plan.md](./deployment-plan.md)).
- [x] Verify: same transaction inputs produce the same `Decision` output as documented in [v1/rule-engine.md](../v1/rule-engine.md) — `DecisionEngineTest` and per-rule boundary tests (fire/no-fire at the exact threshold) added and passing; confirms the persistence swap didn't change rule/engine behavior (including V1's uncapped-above-100 quirk, deliberately preserved until Phase 2).

## Phase 1 — Security: JWT auth, Spring Security, RBAC ✅ implemented

- [x] Add `spring-boot-starter-security` + a JWT library to `pom.xml`. Used `io.jsonwebtoken` (jjwt) 0.12.6 (`jjwt-api`/`jjwt-impl`/`jjwt-jackson`) over Nimbus - smaller surface for the simple "sign and verify HS256" need here. Also added `spring-boot-webmvc-test` (test scope): Spring Boot 4 moved `@AutoConfigureMockMvc`/`@WebMvcTest` out of `spring-boot-test-autoconfigure` into this new module as part of its web-stack split - not anticipated when this checklist was written.
- [x] `User` entity already had `passwordHash`/`enabled`/roles from Phase 0; no change needed here.
- [x] `SecurityUser` (implements `UserDetails`, wraps `User`) + `SecurityUserDetailsService` (implements `UserDetailsService`, loads by username via `UserRepository`).
- [x] `JwtService`: `generateAccessToken(User)`, `getAccessTokenTtlSeconds()`, `parseClaims(token)` / `extractUsername(token)` (both return `Optional`, empty on any parse/verify failure). **Deviation**: no separate `generateRefreshToken()` on this class - refresh-token generation/hashing lives entirely in `RefreshTokenService` instead, since the design treats access tokens (JWT) and refresh tokens (opaque random) as different concerns end to end.
- [x] `JwtAuthenticationFilter` (`OncePerRequestFilter`): reads `Authorization: Bearer`, resolves username via `JwtService`, then always re-loads a fresh `SecurityUser` from the DB (via `SecurityUserDetailsService`) rather than trusting an embedded roles claim - so a role change or account-disable takes effect on the very next request, not only after the access token expires. Swallows expired/tampered/unknown-user tokens (leaves the request unauthenticated) rather than throwing.
- [x] `RefreshToken` entity + `RefreshTokenRepository`; `RefreshTokenService`: `issue`/`rotate`/`revoke`/`revokeAllForUser`, SHA-256 hash only ever persisted.
- [x] `SecurityConfig`: stateless session policy, CSRF disabled (reasoning in the class javadoc), `/api/v2/auth/**` permitAll, `/api/v2/admin/**` → `hasRole("ADMIN")`, everything else → authenticated. `BCryptPasswordEncoder` bean already existed from Phase 0 (`PasswordEncoderConfig`) and is picked up automatically by Spring Security's zero-config `DaoAuthenticationProvider` wiring alongside the single `UserDetailsService` bean. **Note**: the `/api/v2/admin/**` rule protects a URL pattern only - no admin controller exists until Phase 3, so it 404s past the security gate for an admin caller (see `AuthIntegrationTest`); it exists now purely so the 403 path is testable in Phase 1.
- [x] `RestAuthenticationEntryPoint` (401 JSON) + `RestAccessDeniedHandler` (403 JSON), both delegating to a shared `SecurityErrorResponseWriter` — matching [api-design.md](./api-design.md#error-format).
- [x] `AuthController` + `AuthService`: `register`/`login`/`refresh`/`logout`, at `/api/v2/auth` (placed directly at the target V2 path since there's no legacy endpoint to preserve, unlike `/transaction` and `/users`). DTOs (`RegisterRequest`, `LoginRequest`, `AuthResponse`) created without Bean Validation annotations, as planned - Phase 4 adds those. Also added `UserAlreadyExistsException` (409) and a generic `AuthenticationException` handler (401, generic "Invalid username or password" message) to `GlobalExceptionHandler` - pulled forward from Phase 4's fuller matrix out of necessity, since login needed *some* correct status code now.
- [x] Refresh-token cookie: httpOnly/`Secure`/`SameSite=Strict`, path scoped to `/api/v2/auth` only, set on register/login/refresh, cleared (maxAge 0) on logout.
- [x] `CorsConfig` rewritten as a `CorsConfigurationSource` bean (not `WebMvcConfigurer`) reading `app.cors.allowed-origins`; `allowCredentials(true)` added since the refresh-token cookie now needs it.
- [x] Retrofitted `TransactionController`/`UserController`: both now take `@AuthenticationPrincipal SecurityUser` and pass the username down to the service layer, which re-fetches the `User` fresh inside its own `@Transactional` method (rather than reusing the detached entity from the filter's already-closed transaction, which would throw `LazyInitializationException` on `restrictedCategories`). `TransactionEvaluateRequest` and the old `UserUpsertRequest` (deleted, replaced by `UserPreferencesUpdateRequest`) no longer carry any user-identifier field - closes [v1/design-decisions.md](../v1/design-decisions.md) item 7. `UserController`'s single endpoint was also renamed `POST /users` → `PUT /users/me/preferences` and now only updates preferences (account creation moved exclusively to `/auth/register`) - a small, necessary reshaping since letting an unauthenticated endpoint create arbitrary accounts once real auth exists would be a security hole.
- [x] Security integration tests (`AuthIntegrationTest`, MockMvc against the real filter chain, not `@WebMvcTest` with security sliced out): register/duplicate-username/login/wrong-password, protected-endpoint with no/valid/tampered token, refresh with missing cookie, refresh rotation invalidating the previous token, logout revocation, and wrong-role vs admin-role against the `/api/v2/admin/**` rule. **Deviation**: "expire→refresh" is tested via rotation-invalidates-the-old-token instead of actually waiting out the access-token TTL - deterministic and exercises the same rotation mechanism without manipulating time in a test. **Not run in this environment** (no Docker available here, same as Phase 0's repository tests) - verified to compile; needs a Docker-capable machine or CI to actually execute.

## Phase 2 — Domain improvements: configurable rules, real daily-limit/frequency logic ✅ implemented

- [x] `RuleConfig` entity + `RuleConfigRepository` already existed from Phase 0; added the service layer now (`RuleConfigService`: `findAll`, `findAllByCode`, `getByCode`, `update`).
- [x] `RuleConfigService.update` exists but no admin controller calls it yet - wiring is still Phase 3, as planned.
- [x] Refactored `SpendingRule`/`AbstractSpendingRule` to a `RuleContext`-based design rather than threading a single `RuleConfig` through the existing signature: `evaluate(Transaction, User, RuleContext)`. `RuleContext` (new, in `engine/`) bundles the resolved weight/enabled/params for *every* rule plus a shared 24-hour lookback of the user's own transactions, built once per evaluation by a new `RuleContextFactory` (one `rule_configs` query, one `decision_thresholds` query, one transaction-history query - not one query per rule). This keeps rules as pure, dependency-free, easily unit-testable classes (construct a `RuleContext` fixture by hand, no Spring/mocks needed) rather than turning them into Spring beans with injected repositories. `AbstractSpendingRule` now takes `(ruleCode, explanation)` instead of `(weight, explanation)`; weight/params are resolved per-call via `context.weightFor(ruleCode)`/`context.paramsFor(ruleCode)`. `RuleEngineConfig`'s bean wiring is unchanged - this was a real test of whether the Strategy-pattern DI design from V1 would survive becoming config-driven, and it did.
- [x] `HighAmountRule` → calendar-day total (midnight-to-midnight in server time, not a rolling 24h window - documented choice, in the class javadoc, matching how consumer banking apps usually describe a "daily limit"). Sums the user's same-day transactions from `RuleContext.getRecentTransactions()` (filtered in-memory, not a dedicated SQL aggregate query - the shared 24h fetch already contains everything needed) plus the current transaction, compares to `dailyLimit`.
- [x] `FrequentTransactionRule` → real velocity check: counts transactions within `params.velocityWindowMinutes` of the transaction being evaluated (not wall-clock "now" - deterministic/testable), fires at `params.velocityCountThreshold` (including the current transaction).
- [x] `NightSpendingRule`: `nightStartHour`/`nightEndHour` now read from `RuleConfig.params` (defaults 23/6 match V1 exactly if a row's params are absent).
- [x] `SensitivityLevelRule`: `sensitivityThreshold` now read from `RuleConfig.params` (default 8).
- [x] `CategoryRestrictionRule`/registration-seeding: **already done in Phase 0/1** - the hardcoded "LUXURY" fallback was removed when `restricted_categories` became a child table (Phase 0), and `AuthService`/`UserService.registerNewUser` already seeds a default `"LUXURY"` row per new user (Phase 1). Nothing left to do here; noting it so this checklist reflects reality.
- [x] `DecisionEngine`: caps the *reported* risk score at 100 (`Math.min(totalRisk, 100)`) but still compares the **uncapped** total against the thresholds for the BLOCK/DELAY decision (capping only changes what's displayed, never the outcome - a transaction scoring 140 and one scoring 80 both correctly BLOCK). Thresholds now come from a new `decision_thresholds` table/`DecisionThresholds` entity (a dedicated table, not two rows folded into `rule_configs`, since a threshold isn't "a rule" - see `V3__add_decision_thresholds.sql`), seeded with V1's exact 80/40.
- [x] `Transaction` entity: added `decisionType`/`riskScore`/`explanation`/`triggeredRules` fields; `TransactionService` now populates all four from the computed `Decision` before saving (V1/Phase 0/1 only ever returned the Decision to the caller - see [v1/database.md](../v1/database.md)). `triggeredRules` is a new `List<TriggeredRuleEntry>` (record: ruleCode/weight/message), persisted via a dedicated `TriggeredRulesConverter`, and reused as-is on the `Decision` API response (structured alternative to parsing the semicolon-joined `explanation` string - see [v1/rule-engine.md](../v1/rule-engine.md)). **Migration note**: `V4__tighten_transaction_decision_columns.sql` backfills any nulls then tightens these four columns to `NOT NULL`, and moves `decision_type` from MySQL `ENUM` to `VARCHAR(10) CHECK (...)` - safer under Hibernate's `ddl-auto=validate` than relying on exact ENUM/JDBC-type-code matching across driver versions. `V5__update_frequent_transaction_rule_params.sql` replaces Phase 0's placeholder `amountThreshold` seed param with the real `velocityWindowMinutes`/`velocityCountThreshold` the rule now reads.
- [x] Unit tests per rule at exact fire/no-fire boundaries, now including the configurable-params paths (custom night window, custom velocity window/threshold, custom sensitivity threshold) and calendar-day aggregation/isolation for `HighAmountRule`; `DecisionEngineTest` covers the 100-point cap, disabled-rule skipping, and threshold boundaries. `RuleConfigServiceTest` (Mockito) added for the new service. `RuleConfigRepositoryTest` extended to check the updated `FREQUENT_TRANSACTION` params; new `DecisionThresholdsRepositoryTest` checks the seeded 80/40. 30 Docker-independent tests pass locally (`mvn test -Dtest=...`); the Testcontainers-backed repository/integration tests are unchanged in nature from Phase 0/1 (still not run in this environment - no local Docker).

## Phase 3 — API surface: advanced transaction history, dashboard, admin endpoints ✅ implemented

- [x] `TransactionController` moved from the legacy `/transaction` path to `/api/v2/transactions`: `POST /evaluate`, `GET /{publicId}` (owner or `ROLE_ADMIN` only - a non-owner gets the same `TransactionNotFoundException` → 404 an unknown `publicId` would, never 403, per [api-design.md](./api-design.md#error-format)). `evaluateAndSave` now returns the saved `Transaction` entity itself (not a bare `Decision`) so evaluate/get-by-id/history all map through the same `TransactionMapper`.
- [x] `GET /api/v2/transactions/history`: pagination via Spring Data's auto-resolved `Pageable` (`page`/`size`/`sort` query params, no manual parsing needed), filters (`from`, `to`, `category`, `merchant`, `decisionType`, `minAmount`, `maxAmount`) via `TransactionSpecifications` - including a new `byDecisionType` builder, which Phase 0 had explicitly deferred until `Transaction.decisionType` existed (Phase 2 added it).
- [x] `GET /api/v2/transactions/history/export`: `StreamingResponseBody` CSV (not buffered in memory), capped at 10,000 rows, manually quote/escaped. **Deviation**: not yet audit-logged as a data-export action - the audit writer is Phase 4 work (noted inline in the controller).
- [x] `UserController` moved from `/users` to `/api/v2/users`, split into `GET /me`, `PUT /me/preferences`, `GET/POST/DELETE /me/restricted-categories`.
- [x] `DashboardController` + `DashboardService`: `summary`, `spending-by-category`, `risk-trend`, `top-triggered-rules`, each scoped to the caller by default with an optional `userId` param for `ROLE_ADMIN` cross-user viewing (checked in the service, throwing Spring Security's `AccessDeniedException` directly for a non-admin - no new exception handler needed, it already flows to `RestAccessDeniedHandler` from Phase 1). Aggregates are computed in-memory over one fetched 30-day transaction list per call (consistent with `RuleContextFactory`'s established pattern), not via separate SQL aggregate queries. **Deviation**: the admin cross-user override works but isn't audit-logged yet, same reason as the CSV export.
- [x] `AdminUserController`: `GET /users` (paginated), `GET /users/{id}`, `PATCH /users/{id}/status` - via a new `AdminUserService`, kept separate from `UserService` (which only ever acts on the caller's own account).
- [x] `AdminRuleConfigController`: `GET /rule-configs`, `PUT /rule-configs/{ruleCode}` - the first real caller of Phase 2's `RuleConfigService.update`.
- [x] `AdminAuditLogController`: `GET /audit-logs` with `action`/`from`/`to` filters and pagination, backed by a new `AuditLog` entity/repository/`AuditLogSpecifications` and a read-only `AuditLogService`. Endpoint shell as planned - returns real (currently empty) paginated results; nothing writes to `audit_log` until Phase 4's `@Auditable` aspect.
- [x] `PageResponseDto<T>` with a `from(Page<S>, Function<S,T>)` factory, used by every paginated endpoint above.
- [x] Added MapStruct (`mapstruct` + `mapstruct-processor` via the compiler plugin's `annotationProcessorPaths`) and wrote `TransactionMapper`, `RuleConfigMapper`, `AuditLogMapper` for the three entities with a genuine entity→DTO shape translation introduced this phase. Dashboard DTOs are aggregation results (not direct entity mappings) and `UserProfileResponse` predates this phase, so neither uses MapStruct - a deliberate scope reading of "every entity↔DTO pair introduced this phase," not an oversight.
- [x] Controller integration tests (MockMvc + real security chain, `@SpringBootTest` not `@WebMvcTest`): `TransactionControllerIntegrationTest`, `UserControllerIntegrationTest`, `DashboardControllerIntegrationTest`, `AdminControllerIntegrationTest` (covering all three admin controllers together, since they share setup boilerplate). Fixture transactions use a fixed daytime `occurredAt` throughout to avoid `NightSpendingRule` non-deterministically firing depending on wall-clock test-run time. **Not run in this environment** (no Docker available here, same as every Testcontainers-backed test since Phase 0) - verified to compile; needs a Docker-capable machine or CI to actually execute.

## Phase 4 — Cross-cutting: validation, global exception handling, logging, audit logging ✅ implemented

- [x] Added Bean Validation (`spring-boot-starter-validation`) to every request DTO: `RegisterRequest`, `LoginRequest` (presence-only, so a malformed request is 400 distinct from a wrong-credentials 401), `UserPreferencesUpdateRequest`, `TransactionEvaluateRequest`, `RestrictedCategoryRequest`, `RuleConfigUpdateRequest`. **Cleanup enabled by this**: `UserPreferencesUpdateRequest` dropped `restrictedCategories` (api-design.md always scoped this endpoint to dailyLimit/nightSpendingAllowed/sensitivityLevel only - restricted-category management already has its own Phase 3 endpoints) and changed `sensitivityLevel` from primitive `int` to `Integer` (the old "0 means use the default of 5" sentinel was incompatible with real `@Min(1)`/`@Max(10)` validation). `AuthService.register`/`login`'s manual null/blank checks were removed as redundant now that `@Valid` guarantees them at the controller boundary.
- [x] No custom validator was added - every constraint needed (`@NotBlank`, `@Email`, `@Size`, `@Min`/`@Max`, `@DecimalMin`) is a plain single-field rule; none of this project's DTOs have cross-field or combined-logic validation that would need one. Matches the task's own conditional wording ("if a plain @Min/@Max pair isn't expressive enough") - it is enough here.
- [x] Expanded `GlobalExceptionHandler` with the full matrix: `MethodArgumentNotValidException`/`ConstraintViolationException` → 400 with `fieldErrors`; `AccessDeniedException` → 403 (using the exception's own message - reachable for manually-thrown cases like `DashboardService`'s admin check, since URL-level `authorizeHttpRequests` denials never reach a controller advice at all); `JwtException` → 401 (defensive - `JwtService` already swallows this internally, so it's currently unreachable in practice); `DataIntegrityViolationException` → 409. `AuthenticationException` → 401 already existed from Phase 1. Every `ErrorResponse` now also carries `correlationId` (from MDC).
- [x] New `DatabaseOperations.execute(Supplier, message)` utility centralizes "let `DataIntegrityViolationException` through as-is, wrap everything else as `DatabaseOperationException`" - applied to every repository-`.save()` call site (`UserService`, `TransactionService`, `AdminUserService`, `RuleConfigService`), closing the V1 `UserRepository`-vs-`TransactionRepository` asymmetry structurally rather than by convention.
- [x] `CorrelationIdFilter`: generates or propagates `X-Request-Id`, stores it in MDC, echoes it as a response header. Registered via `@Order(HIGHEST_PRECEDENCE)` so it runs before Spring Security's own chain (even 401/403 responses carry a correlation ID). **Side finding while wiring this up**: `JwtAuthenticationFilter` (a `@Component` also manually added to the security chain) was being double-registered by Spring Boot's generic filter auto-registration - harmless in practice (the filter is idempotent) but wasteful; fixed with a `FilterRegistrationBean.setEnabled(false)` in a new `FilterRegistrationConfig`.
- [x] `logback-spring.xml`: pattern encoder by default, `logstash-logback-encoder` JSON encoder under `<springProfile name="docker | prod">` (forward-compatible infrastructure - the actual profile split is still Phase 6, same pattern as Phase 1's early `/api/v2/admin/**` rule).
- [x] `AuditLog` entity/repository already existed from Phase 3 (built then for the read-only endpoint shell). Added `AuditLogService.record(action, entityType, entityId, metadata)`: `@Transactional(REQUIRES_NEW)` plus an internal try/catch that never lets an exception escape - both together guarantee a failed audit write neither fails nor rolls back the caller's transaction. **Deviation**: stays on the calling thread ("separate transaction," not "separate transaction/thread" per the task's literal wording) - real async dispatch would need a thread pool, cross-thread MDC propagation, and its own failure handling, which is real complexity for marginal benefit at this project's scale.
- [x] `@Auditable(action, entityType, failureAction)` + `AuditAspect` (`@AfterReturning`/`@AfterThrowing`, not `@Around` - simpler for this "log after the fact" use case, needs no control over the invocation itself). `extractEntityId` is a small finite dispatch over User/Transaction/RuleConfig, not generic reflection - there are only a few audited types. Added `spring-boot-starter-aspectj` (Spring Boot 4 renamed `spring-boot-starter-aop`).
- [x] Annotated: `AuthService.register` (`USER_REGISTERED`), `AuthService.login` (`LOGIN_SUCCESS`/`LOGIN_FAILURE` via `failureAction`), `UserService.updatePreferences`/`addRestrictedCategory`/`removeRestrictedCategory`, `TransactionService.evaluateAndSave` (`TRANSACTION_EVALUATED`) and `.searchForExport` (`TRANSACTION_HISTORY_EXPORTED` - closes the Phase 3 CSV-export deferral), `AdminUserService.updateStatus`, `RuleConfigService.update`. `DashboardService`'s admin cross-user view calls `AuditLogService.record` **directly** instead of via `@Auditable` - only that one conditional branch (admin overriding `targetUserId`) is audit-worthy, not every routine self-view, and `@Auditable` has no "only sometimes" support (closes the other Phase 3 deferral).
- [x] `AuditLogServiceTest` (Mockito, no Spring context) verifies a repository throwing on `save` does not propagate out of `record(...)`.
- [x] `AdminAuditLogController` needed no changes - it already read through the real `AuditLogRepository` since Phase 3; it now actually returns non-empty results once real actions occur.
- [x] **New Docker-independent tests, all passing locally** (64 total across the project, up from 30): `GlobalExceptionHandlerTest` (14 cases covering the full exception matrix, constructed directly with a fake `HttpServletRequest` - no Spring context needed), `CorrelationIdFilterTest` (4 cases including MDC-cleared-even-on-exception), `AuditLogServiceTest` (3 cases), `RequestValidationTest` (13 cases exercising real Bean Validation constraints on the key DTOs via a plain `Validator`). Also added one Testcontainers-backed end-to-end test (`TransactionControllerIntegrationTest`) proving a 400 response carries both `fieldErrors` and a correlation ID via a real HTTP round-trip - not run in this environment (no local Docker) but verified to compile.

## Phase 5 — Documentation & developer experience: OpenAPI/Swagger ✅ implemented

- [x] Added `springdoc-openapi-starter-webmvc-ui` **3.1.0** - deliberately not the Maven Central "latest" search result (2.8.6, stale index) or even 2.9.0/3.0.x; 3.1.0 is the first release whose changelog explicitly says "Upgrade Spring Boot to version 4.1.0", making it the correct line for this project's Spring Boot 4 baseline. Verified directly against Maven Central (not just the search API) that the artifact is genuinely published before depending on it.
- [x] `OpenApiConfig`: title/version/description, a global `bearerAuth` HTTP-bearer/JWT security scheme. `@Tag` added to every controller matching api-design.md's section headers (Auth, Users, Transactions, Dashboard, Admin - the three admin controllers share one "Admin" tag so they group together in the UI).
- [x] `@Operation`/`@Parameter`/`@ApiResponses` added where the generated defaults genuinely don't explain themselves: the transaction get-by-id 404-vs-403 ownership nuance, the dashboard `userId` admin-override param (repeated per endpoint since each has its own), the rule-config `params` field's rule-specific keys (also a `@Schema(example=...)` on `RuleConfigUpdateRequest.params`, since a raw `Map<String,Object>` renders with no hint of what to put in it), the CSV export's row cap and audit-logging, and login/register's error semantics. `AuthController` overrides the global `bearerAuth` requirement back off via a class-level `@SecurityRequirements` (empty) - none of its endpoints take an access token; refresh/logout authenticate via the httpOnly cookie instead. Deliberately did *not* blanket-annotate every single endpoint - only ones with non-obvious behavior, matching the project's stated documentation philosophy (why over what).
- [x] Profile-gated via `application.properties`' multi-document `prod` block (`spring.config.activate.on-profile=prod`): `springdoc.api-docs.enabled=false` and `springdoc.swagger-ui.enabled=false`. Chose full disable over `ROLE_ADMIN`-gating - gating would still need Swagger UI's static HTML/JS to load unauthenticated (defeating the point), and disabling is one of the two options the original task explicitly allowed. `SecurityConfig` permits `/swagger-ui/**`, `/swagger-ui.html`, `/v3/api-docs/**` so the UI actually loads without a token in the profiles where it's enabled.
- [ ] **Manual pass not completed** - genuinely can't be, in this environment. Verified as much as possible without a real datasource: dependency resolution succeeds with no version conflicts, full compilation succeeds, and a bounded `spring-boot:run` attempt shows the Spring context progressing through bean creation (JWT filter, repositories, security beans, etc. all wire up successfully) before failing at the *expected*, unrelated wall - no local MySQL. No springdoc-related bean-creation or classpath error appeared anywhere in that log. This is meaningful but incomplete evidence: springdoc's own beans could still fail later in the startup sequence, and Swagger UI's actual HTML/JS were never fetched or rendered. **This needs a real run on a Docker-capable machine** (`docker compose up`, once Phase 6 exists, or any environment with a real MySQL) to truly confirm. Flagged here rather than silently marked done - see also the version-compatibility note below.
- **Known risk worth flagging**: springdoc 3.1.0's own changelog targets Spring Boot **4.1.0**; this project pins Spring Boot **4.0.8** (still a minor version behind). Everything checked so far (dependency resolution, compilation, partial context startup) is consistent with compatibility, but this hasn't been fully proven end-to-end. Worth re-verifying (or bumping the Spring Boot parent version) once a real environment is available. **Update (2026-08-28)**: the parent was bumped `4.0.2` → `4.0.8` (latest 4.0.x patch) - compiles clean and the full non-Docker test suite (139 tests) still passes, but the minor-version gap to springdoc's targeted 4.1.0 remains, so this item stays open.

## Phase 6 — Packaging & delivery: Docker, GitHub Actions, deployment ✅ implemented

- [x] Backend multi-stage `Dockerfile` (maven build stage → JRE runtime stage, non-root user) per [deployment-plan.md](./deployment-plan.md#backend-dockerfile-multi-stage). Uses `maven:3.9-eclipse-temurin-17` to build via the wrapper, then `eclipse-temurin:17-jre-alpine` to run as a dedicated non-root `impulselock` user. A root `.dockerignore` keeps `target/`, `frontend/`, `docs/`, and `.git/` out of the build context.
- [x] Frontend multi-stage `Dockerfile` (node build stage → nginx serve stage), nginx config (`frontend/nginx.conf`) reverse-proxying `/api/v2/*` to the `backend` compose service. This replaces CRA's dev-only `"proxy"` field in `package.json`, which has no effect on a production build.
- [x] `docker-compose.yml`: `mysql` (named volume, `mysqladmin ping` healthcheck), `backend` (`depends_on: condition: service_healthy`, Flyway runs automatically on boot as in every other environment, `/actuator/health`-based healthcheck), `frontend` (waits on backend's healthcheck).
- [x] `.env.example` at the repo root with every required variable (`MYSQL_ROOT_PASSWORD`, `JWT_SECRET`, plus the already-optional `JWT_ACCESS_TOKEN_TTL_MINUTES`/`JWT_REFRESH_TOKEN_TTL_DAYS`/`APP_CORS_ALLOWED_ORIGINS`); `.env` was already git-ignored (see root `.gitignore`, added ahead of need back in Phase 0).
- [x] Added `spring-boot-starter-actuator`; `management.endpoints.web.exposure.include=health` exposes only `/actuator/health` (`show-details=never` — no internals leaked), permitted unauthenticated in `SecurityConfig` since the compose healthcheck carries no JWT.
- [x] **Real fix, not just packaging**: `spring.datasource.url`/`username`/`password` are now `${SPRING_DATASOURCE_*:...}` env-var-driven (same pattern as `app.jwt.secret` since Phase 1), closing [v1/design-decisions.md](../v1/design-decisions.md)'s "plaintext DB credentials committed" item for good. `docker-compose.yml` points these at the `mysql` service hostname instead of `localhost`, and sets `SPRING_PROFILES_ACTIVE=docker` so Phase 4's `logback-spring.xml` `docker | prod` JSON-logging block actually activates in containers (springdoc stays enabled, since its own profile gate is `prod`-only).
- [x] `.github/workflows/ci.yml`: `backend` job (`setup-java` 17/temurin, `./mvnw -B verify` — the GitHub-hosted runner provides Docker, so this actually exercises the Testcontainers-based repository/controller tests this project's local sandbox never could — then uploads the JaCoCo HTML report as a build artifact), `frontend` job (`setup-node` 20, `npm ci`, `npm test -- --watchAll=false`, `npm run build`). Both run in parallel on every push/PR to `main`.
- [x] ~~`.github/workflows/cd.yml`: on push to `main` or a `v*.*.*` tag, builds and pushes both `backend` and `frontend` images to GHCR (tagged by git SHA, plus semver on release tags via `docker/metadata-action`). Includes a documented `deploy` job stub (`if: false`) — no hosting target has been chosen yet, left as an explicit placeholder rather than guessed at.~~ **Since removed.** Once Render + Vercel were chosen, they built their own images from the Git integration and nothing pulled the GHCR ones, while the `deploy` stub showed a misleading *Skipped* step in every run for a deploy that had already happened another way. `ci.yml` is now the only workflow and is a pure quality gate — see [deployment-plan.md § No CD workflow](./deployment-plan.md).
- [x] Added `jacoco-maven-plugin` to `pom.xml` (not explicitly listed in the original Phase 6 wording above, but needed to produce the coverage report CI now uploads, and referenced by [testing-strategy.md](./testing-strategy.md#coverage-target)). **Deviation**: pinned to `0.8.15`, not the initially-tried `0.8.12` — this local machine's default JDK is 25 (`java -version` → `25.0.1`), and JaCoCo's on-the-fly bytecode instrumentation of Mockito's dynamically-generated mock classes failed under `0.8.12` with `Unsupported class file major version 69` (Java 25's class file version); `0.8.15` (Maven Central's latest as of this phase) instruments those classes correctly. No coverage-percentage gate was added — the concrete minimum is still a deliberate decision to make once CI has run against real numbers a few times (per testing-strategy.md), not something to invent here.
- [ ] **Verify from a clean checkout that `docker compose up` actually brings up the full stack — not done.** Same constraint as every Docker/Testcontainers gap flagged in Phases 0–5: this sandbox has no Docker daemon (confirmed repeatedly all session). What *was* verified without Docker: `./mvnw -o compile`/`test-compile` succeed with the new `spring-boot-starter-actuator`/`jacoco-maven-plugin` dependencies and the `SecurityConfig`/`application.properties` changes, and the full Docker-independent test suite (64 tests) still passes with the same 0 failures/0 real errors as every prior phase (the 1 reported "error" in a stale intermediate run was `ImpulseLockApplicationTests`, the pre-existing Docker-dependent default context-load test, not a regression). The Dockerfiles/compose file were written directly against `docs/v2/deployment-plan.md`'s design and reviewed for correctness (base image choices, healthcheck commands, service dependency ordering, env var wiring) but the actual `docker compose up` end-to-end run needs a Docker-capable machine.
- [ ] **Mark CI as a required status check on `main` — not done, and deliberately left for the user.** This is a GitHub repository *settings* change (branch protection rules), not a code change, and only takes effect once `.github/workflows/ci.yml` has actually run at least once on GitHub (which needs a push this session doesn't perform, per the standing "commit but don't push" instruction). It's also a repo-governance decision, not an implementation one — left as a manual follow-up: once pushed, go to Settings → Branches → branch protection rule for `main` → require the `backend`/`frontend` status checks.

## Phase 7 — Frontend: improved dashboard, advanced history UI, auth flows ✅ implemented

- [x] `AuthContext`/hook (`src/auth/AuthContext.js`): holds `user`, `roles`, and `status` in React state; `login`/`register`/`logout`/`hasRole`/`isAdmin`. The access token itself lives only inside `api.js` (module-level variable, not React state) so `apiFetch` can read/refresh it without a render cycle; `AuthContext` and `api.js` stay in sync via `registerSessionHandlers({ onAuthUpdated, onSessionExpired })`, a small callback registry that lets the plain-JS `api.js` module push session changes into React without depending on React itself. On mount, `AuthProvider` calls `/auth/refresh` once to silently restore a session from the httpOnly cookie (access tokens are never persisted client-side, per security-design.md), showing a brief loading state meanwhile.
- [x] `LoginForm`, `RegisterForm` components - `RegisterForm` renders server-side field errors (`ErrorResponse.fieldErrors`) next to their field.
- [x] Evolved `api.js`: every request attaches `Authorization: Bearer <token>` automatically; a 401 from any non-auth endpoint triggers exactly one silent `/auth/refresh` + retry (guarded against refreshing off of `/auth/refresh`'s own 401, so it can't loop); `credentials: 'include'` is always sent so the refresh-token cookie travels correctly. Also added `downloadFile()` (blob response + synthetic anchor click) for CSV export, since that response isn't JSON.
- [x] Role gating: no router was introduced (plain conditional tab render remained sufficient - four tabs became five, with `Admin` appended only when the decoded token carries `ROLE_ADMIN`). Roles are read by base64-decoding the JWT payload client-side (`AuthContext.decodeRoles`) - **purely a UI-gating convenience, not a security boundary**; the real enforcement is still `SecurityConfig`'s `hasRole("ADMIN")` on `/api/v2/admin/**` (Phase 1).
- [x] `Dashboard` view: summary tiles (transaction count, total spend, avg risk score, allow/delay/block breakdown), a spending-by-category bar list, a risk-trend line chart, and a top-triggered-rules bar list - consuming Phase 3's four dashboard endpoints in parallel (`Promise.all`). **Deviation**: no chart library was added - both "charts" are hand-rolled (CSS-width bars for category/rules, a small inline SVG polyline for the risk trend) to avoid a new dependency for what's structurally simple data; revisit if a future dashboard needs anything more elaborate (zoom, tooltips, multi-series).
- [x] `TransactionHistory` view: the same filter set as `/transactions/history` (from/to/category/merchant/decisionType/min-max amount), clickable sortable column headers (occurredAt/amount/riskScore), pagination, and a CSV export button wired to `/transactions/history/export` via `downloadFile`. Filters only take effect on "Apply filters" (a separate `appliedFilters` state, not live-fetching per keystroke) - the same pattern is used in `AdminAuditLogs`.
- [x] `TransactionForm`/`UserPreferencesForm` rewritten: `userId` dropped entirely from both (server resolves the caller from the token). `UserPreferencesForm` now loads the current profile via `GET /users/me` on mount instead of starting blank, and restricted categories moved off the old bulk comma-separated text field onto the granular `/users/me/restricted-categories` endpoints (add/remove take effect immediately, no separate "save" step for those, matching the endpoints' own semantics).
- [x] `AdminPanel` (`src/components/admin/`): tabbed `AdminUsers` (paginated list + enable/disable toggle), `AdminRuleConfigs` (inline weight/enabled/params-JSON editor per rule), `AdminAuditLogs` (action/from/to filter + paginated table) - rendered only when `isAdmin` is true.
- [x] Component tests (React Testing Library) + API-mocked tests for every new/changed component - **31 tests across 13 suites**, all passing, including the explicitly-called-for 401-triggers-refresh test (`api.test.js`, 3 cases covering success, refresh failure, and the no-nested-loop guard) and a login-flow test (`LoginForm.test.js`). **Deviation from the literal wording above**: used **MSW v1** (`msw@1.3.5`) + the `whatwg-fetch` polyfill, not MSW v2. This environment's toolchain is react-scripts 5.0.1 / Jest 27 / jsdom 16 (all pinned by react-scripts, not upgraded), which has no global `fetch`/`Request`/`Response`/`TextEncoder`/`ReadableStream`/`BroadcastChannel` - verified directly by probing the test environment before choosing an approach. MSW v2 requires several of those natively and would need extensive manual polyfilling to even boot; MSW v1's Node interceptor works against `whatwg-fetch`'s XHR-based `fetch` polyfill instead, which is the well-established combination for this exact (now-old) toolchain. Documented here rather than silently deviating from the plan.
- Also fixed a real bug this testing caught: `AdminAuditLogs`'s "Apply filters" button silently did nothing when the page number didn't change (its fetch effect depended only on `[page]`, not the filter values) - the same pattern in `TransactionHistory` happened to still work because its `buildQuery` callback was itself a dependency, but both were changed to a consistent `appliedFilters`-state pattern so filtering never depends on the page number as a side effect.
- Verified: `npm test -- --watchAll=false` (31/31 passing) and `npm run build` (compiles cleanly, no lint warnings) both run clean from this checkout.

## Phase 8 — Hardening & polish ✅ implemented

- [x] Reviewed the JaCoCo coverage report against the four floor packages named in [testing-strategy.md](./testing-strategy.md#coverage-target) and closed the real gaps found (not padding — every added test asserts a genuine, previously-unverified behavior):
  - `service/`: **15.1% → 91.5%** line coverage. `TransactionService`, `AuthService`, `UserService`, `DashboardService`, `AdminUserService`, and `DatabaseOperations` had **zero** unit coverage before this phase (only reachable via Testcontainers integration tests this sandbox can't run) — added `TransactionServiceTest`, `AuthServiceTest`, `UserServiceTest`, `DashboardServiceTest`, `AdminUserServiceTest`, `DatabaseOperationsTest` (Mockito-based, matching the existing `RuleConfigServiceTest`/`AuditLogServiceTest` style).
  - `security/`: **52.2% → 98.6%**. Added `LoginRateLimiterTest`, `JwtServiceTest`, `RefreshTokenServiceTest`, `SecurityUserDetailsServiceTest`, `SecurityUserTest`, `SecurityErrorResponseWriterTest`, `RestAuthenticationEntryPointTest`, `RestAccessDeniedHandlerTest`, `JwtAuthenticationFilterTest` — covers the full JWT lifecycle (issue/parse/expiry/tamper-rejection, rotation, revocation) and the 401/403 JSON-error path testing-strategy.md's "comprehensive" section explicitly calls out.
  - `engine/`: `RuleContextFactory` was at 0% (`DecisionEngine`/`RuleContext` were already well-covered) — added `RuleContextFactoryTest`.
  - `rules/` was already at 96.6% - no changes needed.
  - **139 Docker-independent backend tests now pass** (up from 64 at the start of this phase), plus the pre-existing Testcontainers-based integration/repository tests (compile-verified only, per the standing no-Docker-in-this-sandbox limitation).
- [x] **Implemented** rate limiting on `/auth/login` (not deferred) — new `LoginRateLimiter` (in `security/`), an in-memory, per-username, fixed-window brute-force guard (5 attempts / 15 minutes by default, configurable via `app.security.login-rate-limit.*` properties), wired into `AuthController.login`. Exceeding it throws a new `TooManyLoginAttemptsException`, mapped to **HTTP 429**. Keyed by username only (not IP) — see [security-design.md](./security-design.md#rate-limiting-authlogin) for the reasoning (avoids NAT/shared-network false-positive lockouts; in-memory is sufficient since Phase 6 settled on a single-backend-instance deployment shape). This was originally left as a deferred stretch item pending a deployment-shape decision — that decision is now made, so it's built rather than deferred again.
- [x] Rewrote the top-level `README.md`: Docker-first quickstart (`cp .env.example .env` → `docker compose up --build`) as the primary path, including the manual `user_roles` SQL step needed to get an admin account (there is no seeded/CLI admin provisioning — see below). Manual (non-Docker) setup kept as a documented fallback, corrected to describe how the project's config actually works (env-var overrides on `application.properties`, not a nonexistent `application-local.properties` auto-load mechanism the old `.gitignore` entry implied).
- [x] Final pass reconciling `architecture.md`, `security-design.md`, `database-design.md`, `api-design.md` against the actual built code (done via a dedicated review pass across all four documents together, cross-checked line-by-line against the real controllers/services/entities/migrations/frontend). Real drift found and fixed, e.g.: no `@PreAuthorize` annotation exists anywhere (the docs claimed one) — authorization is enforced via URL-level `SecurityConfig` plus manual `isAdmin()`/ownership checks in service code; `AuditAspect` uses `@AfterReturning`/`@AfterThrowing`, not `@Around`; `transactions.decision_type` is `VARCHAR(10)` + `CHECK` (via `V4`), not the `ENUM` the docs described; `decision_thresholds` had no dedicated documentation section despite being a real table with no config UI; `restricted_categories.category` is stored as-trimmed (compared case-insensitively at read time), not uppercased at write time as claimed; admin provisioning has no seed/CLI/endpoint (documented the actual manual DB step, matching the new README); the "rate limiting out of scope" section is replaced with a description of what's now actually built (see above).
- [x] Confirmed every one of the 12 documented V1 limitations in [v1/design-decisions.md](../v1/design-decisions.md) — **all 12 are fixed, none deferred**:
  1. Daily limit not a true daily aggregate → fixed Phase 2 (`HighAmountRule` calendar-day aggregation).
  2. "Frequent transaction" was a flat threshold → fixed Phase 2 (real velocity window).
  3. Uncapped/additive risk score → fixed Phase 2 (`DecisionEngine` caps at 100).
  4. `restricted_categories` schema mismatch → fixed Phase 2/3 (normalized table + granular endpoints).
  5. No transaction history GET → fixed Phase 3 (`/transactions/{publicId}`, `/transactions/history`).
  6. Inconsistent DB-failure handling → fixed Phase 4 (`DatabaseOperations.execute`, applied uniformly).
  7. No auth/authz → fixed Phase 1 (JWT + Spring Security + RBAC).
  8. No numeric bounds validation → fixed Phase 4 (Bean Validation on every request DTO).
  9. Demo `main()` scenario on startup → fixed Phase 0 (removed).
  10. No automated test coverage → fixed cumulatively, dramatically so this phase (139 backend + 32 frontend tests, up from V1's single smoke test each).
  11. **Tab-switch state bleed** → found still genuinely present during this phase's confirmation pass (the Phase 7 frontend rewrite carried the same bug forward unnoticed - switching between the Transaction/Preferences tabs didn't clear the previous tab's stale result) - **fixed now**: `App.js` gained a `switchTab` helper and a `VIEW_DEFAULTS` map so changing tabs always resets `view` to that tab's own empty state; added a dedicated regression test in `App.test.js`.
  12. Plaintext DB credentials committed → fixed Phase 6 (env-var-driven `SPRING_DATASOURCE_*`).
